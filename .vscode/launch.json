{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch NavFit in Chrome",
            "file": "${workspaceFolder}/navfit-web.html"
        }
    ]
}